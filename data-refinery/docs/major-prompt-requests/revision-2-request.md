Add resume support
Add embeddings option on entities (New vllm instance on docker compose with embedding model)
Add fastmcp MCP to access the neo4j db
Add an app that uses neo4j as tool (fastmcp stdio), and uses to the generate documents or Q&A pairs
IN each case, add number of requested documents as input, and optional 'guidance' input

