{
  "type": "function",
  "function": {
    "name": "move_antennas",
    "description": "Move the robot's antennas. Both left and right antenna positions must be specified. Leave parameters out to keep current position.",
    "parameters": {
      "type": "object",
      "properties": {
        "left": {
          "type": "number",
          "description": "Target position for left antenna in degrees (typically -45 to 45)"
        },
        "right": {
          "type": "number",
          "description": "Target position for right antenna in degrees (typically -45 to 45)"
        },
        "duration": {
          "type": "number",
          "description": "Duration of the movement in seconds",
          "default": 2.0
        }
      },
      "required": [
        "left",
        "right"
      ]
    }
  }
}